# MarkMaid v0.1.5 Release Checklist

This is the mutable release-signoff record for v0.1.5. Public-facing copy lives
in [`v0.1.5.md`](v0.1.5.md).

## Completed

- [x] Version metadata synchronized at `0.1.5`.
- [x] Dated v0.1.5 changelog section and compare link added.
- [x] README features, shortcuts, preview limits, asset scope, and install notes
  reviewed against the shipped UI.
- [x] Stale code-block Download claims replaced with Copy-only wording.
- [x] `CI=true pnpm check` passed: 99 frontend tests, 43 Rust tests,
  TypeScript, production frontend build, Rust formatting, and Clippy.
- [x] Apple Silicon debug `.app` build passed and the executable was confirmed
  as Mach-O arm64.
- [x] Implementation commit `e07f850` pushed to `origin/main`.

## Manual application QA

- [x] Open valid Markdown, Mermaid, and image files through the dialog, drag and
  drop, Finder Open With, second-instance forwarding, Markdown links, and Files.
- [x] Confirm unsupported and oversized files show a visible notice in both
  sidebar modes without launching an external app.
- [x] Exercise lazy restore, retry, close-while-loading, ready reload failure,
  and external-change Reload / Ignore behavior.
- [x] Traverse relative links and anchors across tabs, then verify Back /
  Forward boundaries, scroll restoration, tab close, and Reopen Closed Tab.
- [x] Verify Quick Open unavailable-root and truncated-index messages, the
  200-result display cap, and responsiveness with noise-heavy/deep fixtures.
- [x] Verify allowed local images render and out-of-bound `file://` images use
  the blocked fallback.
- [x] Export HTML and PDF; verify print success, cancellation, print-command
  failure, asset timeout, page-load timeout, and absence of orphaned hidden
  `print-export-*` windows.

## Artifact and publication gates

- [x] Run `CI=true pnpm release:build` to regenerate the complete signed release
  set from the final clean commit.
- [x] Confirm `artifacts/v0.1.5/` contains exactly:
  `MarkMaid_0.1.5_aarch64.dmg`, `MarkMaid_0.1.5_aarch64.app.zip`, and
  `SHA256SUMS`.
- [x] Verify `shasum -a 256 -c artifacts/v0.1.5/SHA256SUMS` passes.
- [x] Commit and push this release documentation, then confirm the working tree
  is clean and `main` is not ahead of `origin/main`.
- [x] Run `pnpm release:publish -- --dry-run`.
- [x] Confirm the public release body from [`v0.1.5.md`](v0.1.5.md), asset names,
  signing/quarantine note, and v0.1.4...v0.1.5 compare link.
- [ ] Publish v0.1.5, then verify the remote tag, GitHub Release body, all three
  assets, and displayed checksums.

Do not create or push `v0.1.5` until every preceding gate is signed off.
